function checkRequiredDocumentsACA(reqDocs, itemCap) {	var allAttached = true; 	var uploaded = true; 	var msg = "All Attached";	var doctypes = "";	for (category in reqDocs) {		if (uploaded == true) {			var attached = false;			docList = aa.document.getDocumentListByEntity(itemCap, "TMP_CAP"); 			docs = docList.getOutput();			if (docs.isEmpty()) {				var msg = "The required documents are not attached to this application";                                for(reqDoc in reqDocs){                                   msg+="<BR>"+reqDocs[reqDoc];                                }				uploaded = false;			} else {				idocs = docs.iterator();				while (idocs.hasNext()) {					doc = idocs.next(); 					docCategory = doc.getDocCategory();					if (reqDocs[category] == docCategory) {						attached = true;						break;					}				}				if (attached == false) {					doctypes += "<br>" + reqDocs[category]; 					allAttached = false;				}			}		}	}	if (allAttached == false) {		var msg = "Please attach a copy of the following document type(s):" + doctypes;	}	return msg;}