function moveToParent() {	getCapResult = aa.cap.getProjectByChildCapID(capId,"Renewal","Complete");	if (getCapResult.getSuccess()) {		parentArray = getCapResult.getOutput();		if (parentArray.length) {			parCapId = parentArray[0].getProjectID();			result = aa.cap.copyRenewCapDocument(capId, parCapId, currentUserID);		} else {			logDebug( "**WARNING: GetParent found no project parent for this application");			return false;		}	} else { 		logDebug( "**WARNING: getting project parents:  " + getCapResult.getErrorMessage());		return false;	}}