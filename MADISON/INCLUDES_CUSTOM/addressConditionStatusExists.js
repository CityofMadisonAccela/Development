function addressConditionStatusExists(condtype,pStatus)	{	var capAddressResult = aa.address.getAddressByCapId(capId,null);	if (!capAddressResult.getSuccess())		{ logDebug("**WARNING: error getting cap address : " + capAddressResult.getErrorMessage()) ; return false }	var Addresses = capAddressResult.getOutput();	for (zz in Addresses)		{		debugObject(Addresses[zz]);		acResult = aa.addressCondition.getAddressConditions(Addresses[zz].getRefAddressId());		if (!acResult.getSuccess())			{ logDebug("**WARNING: error getting address conditions : " + acResult.getErrorMessage()) ; return false }		acs = acResult.getOutput();		for (ac1 in acs)			if (acs[ac1].getConditionType().equals(condtype) && acs[ac1].getConditionStatus().equals(pStatus)) 		return true;		}	}